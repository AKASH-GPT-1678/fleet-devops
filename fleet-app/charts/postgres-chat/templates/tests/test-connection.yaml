apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "postgres-chat.fullname" . }}-test-connection"
  labels:
    {{- include "postgres-chat.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "postgres-chat.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
