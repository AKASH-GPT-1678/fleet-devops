apiVersion: v1
kind: Service
metadata:
  name: {{ include "postgres-chat.fullname" . }}
  labels:
    {{- include "postgres-chat.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 5432
      protocol: TCP
      name: http
  selector:
    {{- include "postgres-chat.selectorLabels" . | nindent 4 }}
