apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgreschat-pvc
  namespace: fleet-app
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: standard-rwo
