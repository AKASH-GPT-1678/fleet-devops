apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-spring-pvc
  namespace: fleet-app
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: manual
