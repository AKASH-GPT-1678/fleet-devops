apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "postgres-spring.fullname" . }}-test-connection"
  labels:
    {{- include "postgres-spring.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "postgres-spring.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
